name: Deploy Production

on:
  workflow_run:
    workflows: ["CI"]
    branches: [main]
    types: [completed]
  workflow_dispatch:
    inputs:
      ref:
        description: "Branch, tag, or SHA to deploy"
        required: true
        default: main

concurrency:
  group: style-os-mirror-app-deploy-production
  cancel-in-progress: true

env:
  ECR_REPOSITORY: style-os-mirror-prod
  AWS_REGION: eu-west-3
  NODE_ENV: production
  IMAGE_TAG: ${{ github.event.workflow_run.head_sha || github.sha }}

jobs:
  deploy:
    name: Build, Push & Deploy (production)
    if: github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    environment: production

    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.ref || github.event.workflow_run.head_sha }}

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push Docker image
        env:
          NEXT_PUBLIC_API_BASE_URL: ${{ secrets.NEXT_PUBLIC_API_BASE_URL }}
          NEXT_PUBLIC_SOCKET_URL: ${{ secrets.NEXT_PUBLIC_SOCKET_URL }}
          NEXT_PUBLIC_SITE_URL: ${{ secrets.NEXT_PUBLIC_SITE_URL }}
          NEXT_PUBLIC_COMPANION_URL: ${{ secrets.NEXT_PUBLIC_COMPANION_URL }}
          NEXT_PUBLIC_MIRROR_ID: ${{ secrets.NEXT_PUBLIC_MIRROR_ID }}
          NEXT_PUBLIC_KIOSK_DEVICE_SECRET: ${{ secrets.NEXT_PUBLIC_KIOSK_DEVICE_SECRET }}
          NEXT_PUBLIC_MAPBOX_TOKEN: ${{ secrets.NEXT_PUBLIC_MAPBOX_TOKEN }}
          NEXT_PUBLIC_DEVICE_MODE: ${{ secrets.NEXT_PUBLIC_DEVICE_MODE }}
          NEXT_PUBLIC_DOMAIN1: ${{ secrets.NEXT_PUBLIC_DOMAIN1 }}
          NEXT_PUBLIC_DOMAIN2: ${{ secrets.NEXT_PUBLIC_DOMAIN2 }}
          NEXT_PUBLIC_USER1_ACCESS_TOKEN: ${{ secrets.NEXT_PUBLIC_USER1_ACCESS_TOKEN }}
          NEXT_PUBLIC_USER1_REFRESH_TOKEN: ${{ secrets.NEXT_PUBLIC_USER1_REFRESH_TOKEN }}
          NEXT_PUBLIC_USER2_ACCESS_TOKEN: ${{ secrets.NEXT_PUBLIC_USER2_ACCESS_TOKEN }}
          NEXT_PUBLIC_USER2_REFRESH_TOKEN: ${{ secrets.NEXT_PUBLIC_USER2_REFRESH_TOKEN }}
          NEXT_PUBLIC_MODEL_MALE_URL: ${{ secrets.NEXT_PUBLIC_MODEL_MALE_URL }}
          NEXT_PUBLIC_MODEL_FEMALE_URL: ${{ secrets.NEXT_PUBLIC_MODEL_FEMALE_URL }}
        run: |
          ECR="${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}"
          docker build \
            --build-arg NEXT_PUBLIC_API_BASE_URL="${NEXT_PUBLIC_API_BASE_URL}" \
            --build-arg NEXT_PUBLIC_SOCKET_URL="${NEXT_PUBLIC_SOCKET_URL}" \
            --build-arg NEXT_PUBLIC_SITE_URL="${NEXT_PUBLIC_SITE_URL}" \
            --build-arg NEXT_PUBLIC_COMPANION_URL="${NEXT_PUBLIC_COMPANION_URL}" \
            --build-arg NEXT_PUBLIC_MIRROR_ID="${NEXT_PUBLIC_MIRROR_ID}" \
            --build-arg NEXT_PUBLIC_KIOSK_DEVICE_SECRET="${NEXT_PUBLIC_KIOSK_DEVICE_SECRET}" \
            --build-arg NEXT_PUBLIC_MAPBOX_TOKEN="${NEXT_PUBLIC_MAPBOX_TOKEN}" \
            --build-arg NEXT_PUBLIC_DEVICE_MODE="${NEXT_PUBLIC_DEVICE_MODE}" \
            --build-arg NEXT_PUBLIC_DOMAIN1="${NEXT_PUBLIC_DOMAIN1}" \
            --build-arg NEXT_PUBLIC_DOMAIN2="${NEXT_PUBLIC_DOMAIN2}" \
            --build-arg NEXT_PUBLIC_USER1_ACCESS_TOKEN="${NEXT_PUBLIC_USER1_ACCESS_TOKEN}" \
            --build-arg NEXT_PUBLIC_USER1_REFRESH_TOKEN="${NEXT_PUBLIC_USER1_REFRESH_TOKEN}" \
            --build-arg NEXT_PUBLIC_USER2_ACCESS_TOKEN="${NEXT_PUBLIC_USER2_ACCESS_TOKEN}" \
            --build-arg NEXT_PUBLIC_USER2_REFRESH_TOKEN="${NEXT_PUBLIC_USER2_REFRESH_TOKEN}" \
            --build-arg NEXT_PUBLIC_MODEL_MALE_URL="${NEXT_PUBLIC_MODEL_MALE_URL}" \
            --build-arg NEXT_PUBLIC_MODEL_FEMALE_URL="${NEXT_PUBLIC_MODEL_FEMALE_URL}" \
            -t "$ECR:${{ env.IMAGE_TAG }}" -t "$ECR:latest" .
          docker push "$ECR:${{ env.IMAGE_TAG }}"
          docker push "$ECR:latest"

      - name: Write env file
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ${{ secrets.AWS_REGION }}
          NODE_ENV: ${{ env.NODE_ENV }}
          PORT: 3000
        run: |
          cat > ${{ github.workspace }}/mirror-app.env << EOF
          OPENAI_API_KEY=${OPENAI_API_KEY}
          AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
          AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
          AWS_REGION=${AWS_REGION}
          NODE_ENV=${NODE_ENV}
          PORT=${PORT}
          EOF

      - name: Copy env file to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          source: mirror-app.env
          target: /home/ec2-user/

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          command_timeout: 20m
          script: |
            set -e
            chmod 600 /home/ec2-user/mirror-app.env
            aws ecr get-login-password --region ${{ env.AWS_REGION }} | docker login --username AWS --password-stdin ${{ steps.login-ecr.outputs.registry }}
            docker pull ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ env.IMAGE_TAG }}
            docker stop mirror-app 2>/dev/null || true
            docker rm   mirror-app 2>/dev/null || true
            docker run -d --name mirror-app --restart unless-stopped \
              --env-file /home/ec2-user/mirror-app.env \
              -p 3000:3000 \
              ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ env.IMAGE_TAG }}
            docker image prune -f
