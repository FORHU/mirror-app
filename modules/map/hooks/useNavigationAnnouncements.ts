"use client";

// Turn-by-turn voice announcements were tied to moving-user navigation,
// which has been removed. The mirror is stationary — no announcements needed.
export function useNavigationAnnouncements() {}
